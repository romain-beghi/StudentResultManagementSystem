### To build and launch the Spring Boot backend:

1 - `mvn clean install` \
2 - `mvn spring-boot:run`

### To build and launch the React frontend:

3 - `cd ./frontend` \
4 - `npm start` \
5 - `Access UI at http://localhost:3000/`
